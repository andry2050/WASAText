openapi: 3.0.3
servers:
  - url: "http://localhost:3000"
info:
  title: WASAText
  description: WASAText project
  version: 1.0.0
paths:
  /session:
    post:
      tags: ["Login"]
      summary: Logs in the user
      description: |-
        If the user does not exist, it will be created,
        and an identifier is returned.
        If the user exists, the user identifier is returned.
      operationId: doLogin
      requestBody:
        description: User details
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Username" }
        required: true
      responses:
        '201':
          description: User log-in action successful
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UserID" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
        
      security:
        - bearerAuth: []

  /users:
    parameters:
      - name: search
        in: query
        description: The username (or a part of it) to search.
        schema: { $ref: "#/components/schemas/Username" }
        required: true

    get:
      tags: ["User"]
      operationId: searchUsers
      summary: Get the list of users
      description: |-
        Get the list of Users that match with the username specified in the query.
      
      responses:
        '200':
          description: List of registered users
          content:
            application/json:
              schema: { $ref: "#/components/schemas/UsersList" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '401': { $ref: "#/components/responses/Unauthorized" }
        '403': { $ref: "#/components/responses/Forbidden" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}:
    parameters:
      - name: userId
        in: path
        description: The ID of the user who is setting the username.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true

    patch:
      tags: ["User"]
      summary: Set my username
      operationId: setMyUserName
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Username" }
        required: true
      responses:
        '204': { description: Username set successfully }
        '400': { $ref: "#/components/responses/BadRequest" }
        '401': { $ref: "#/components/responses/Unauthorized" }
        '403': { $ref: "#/components/responses/Forbidden" }
        '500': { $ref: "#/components/responses/InternalServerError" }

      security:
        - bearerAuth: []
  
  /users/{userId}/photo:
    parameters:
      - name: userId
        in: path
        description: The ID of the user who is setting a photo.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
    patch:
      tags: ["User"]
      summary: Set my profile photo
      operationId: setMyPhoto
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Photo" }   
        required: true           
      responses:
        '204': { description: Profile photo updated successfully}
        '400': { $ref: "#/components/responses/BadRequest" }
        '403': { $ref: "#/components/responses/Forbidden" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/conversations:
    parameters:
      - name: userId
        in: path
        description: The ID of the user who is searching the conversation.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true

    get:
      tags: ["Conversations"]
      summary: Get user's conversations
      operationId: getMyConversations
      responses:
        '200':
          description: List of conversations
        '400': { $ref: "#/components/responses/BadRequest" }
        '401': { $ref: "#/components/responses/Unauthorized" }
        '403': { $ref: "#/components/responses/Forbidden" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/conversations/{conversationId}:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: conversationId
        in: path
        description: The ID of the conversation.
        schema: { $ref: "#/components/schemas/ConversationID" }
        required: true      
      
    get:
      tags: ["Conversations"]
      summary: Get a conversation by ID
      operationId: getConversation        
      responses:
        '200':
          description: Conversation details
          content:
            application/json: 
              schema: { $ref: "#/components/schemas/Message" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []
  
  /users/{userId}/messages:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
    post:
      tags: ["Messages"]
      summary: Send a new message
      operationId: sendMessage
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Message" }
        required: true
      responses:
        '200':
          description: Message sent successfully
          content:
            application/json: 
              schema: { $ref: "#/components/schemas/Message" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/messages/{messageId}/forward:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: messageId
        in: path
        description: The ID of the message forwarded.
        schema: { $ref: "#/components/schemas/MessageID" }
        required: true

    post:
      tags: ["Messages"]
      summary: Forward a message
      operationId: forwardMessage
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                conversationId: { $ref: "#/components/schemas/ConversationID" }
        required: true
      responses:
        '204': { description: Message forwarded successfully }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }

      security:
        - bearerAuth: []

  /users/{userId}/messages/{messageId}/comment:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: messageId
        in: path
        description: The ID of the message is commented.
        schema: { $ref: "#/components/schemas/MessageID" }
        required: true

    post:
      tags: ["Comment"]
      summary: Comment on a message
      operationId: commentMessage
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Comment" }
        required: true
      responses:
        '200':
          description: Comment added successfully
          content:
            application/json: 
              schema: { $ref: "#/components/schemas/Comment" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }

      security:
        - bearerAuth: []

  /users/{userId}/messages/{messageId}/comment/{commentId}:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: messageId
        in: path
        description: The ID of the message is commented.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: commentId
        in: path
        description: The ID of the message is commented.
        schema: { $ref: "#/components/schemas/CommentID" }
        required: true

    delete:
      tags: ["Comment"]
      summary: Remove a comment from a message
      operationId: uncommentMessage
      responses:
        '204': { description: Comment removed successfully }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/messages/{messageId}:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: messageId
        in: path
        description: The ID of the message deleted.
        schema: { $ref: "#/components/schemas/MessageID" }
        required: true

    delete:
      tags: ["Messages"]
      summary: Delete a message
      operationId: deleteMessage
      responses:
        '204': { description: Message deleted successfully }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }

      security:
        - bearerAuth: []

  /users/{userId}/groups:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
    post:
      tags: ["Groups"]
      summary: Add user to group
      operationId: addToGroup
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/User" }
        required: true
      responses:
        '200':
          description: User added to group successfully
          content:
            application/json: 
              schema: { $ref: "#/components/schemas/UserID" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/groups/{groupId}:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: groupId
        in: path
        description: The ID of the group.
        required: true
        schema: { $ref: "#/components/schemas/GroupID" }

    delete:
      tags: ["Groups"]
      summary: Leave a group
      operationId: leaveGroup
      responses:
        '204': { description: Successfully left the group }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }

      security:
        - bearerAuth: []

  /users/{userId}/groups/{groupId}/name:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: groupId
        in: path
        description: The ID of the group.
        required: true
        schema: { $ref: "#/components/schemas/GroupID" }

    patch:
      tags: ["Groups"]
      summary: Set group name
      operationId: setGroupName
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                groupName: { $ref: "#/components/schemas/GroupName" }
        required: true
      responses:
        '204': { description: Group name updated successfully }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
      
      security:
        - bearerAuth: []

  /users/{userId}/groups/{groupId}/photo:
    parameters:
      - name: userId
        in: path
        description: The ID of the user.
        schema: { $ref: "#/components/schemas/UserID" }
        required: true
      - name: groupId
        in: path
        description: The ID of the group.
        required: true
        schema: { $ref: "#/components/schemas/GroupID" }

    patch:
      tags: ["Groups"]
      summary: Set group photo
      operationId: setGroupPhoto
      requestBody:
        content:
          application/json:
            schema: { $ref: "#/components/schemas/Photo" }
        required: true
      responses:
        '200':
          description: Group photo updated successfully
          content:
            application/json: 
              schema: { $ref: "#/components/schemas/Photo" }
        '400': { $ref: "#/components/responses/BadRequest" }
        '500': { $ref: "#/components/responses/InternalServerError" }
        
      security:
        - bearerAuth: []


components:
  schemas:
    Username: #Rappresenta l'username dell'utente
      description: A user's nickname
      type: object
      properties:
        name:
          description: Any string between 3 and 16 chars
          type: string 
          example: "Maria"
          pattern: '^.*?$'
          minLength: 3 
          maxLength: 16

    UserID: #Rappresenta l'identificativo dell'utente
      description: A user's id
      type: integer
      example: 123456

    User:
      description: Represent a user
      type: object
      properties:
        userId: { $ref: "#/components/schemas/UserID" }
        username: { $ref: "#/components/schemas/Username" }

    UsersList:
      title: Users List
      description: |-
        A list of users for many responses.
        It's used to get a list of users for the search 
      type: array
      items:
        type: object
        description: User
        properties:
          user: { $ref: "#/components/schemas/User" }
      minItems: 0
      maxItems: 100
      
    GroupName:
      description: A group name
      type: object
      properties:
        name:
          description: Any string between 1 and 16
          type: string
          example: "WasaGroup 24/25"
          pattern: '^.*?$'
          minLength: 1
          maxLength: 16

    GroupID: 
      description: A group's id
      type: integer
      example: 123456

    Group:
      description: Represent a group
      type: object
      properties:
        groupId: { $ref: "#/components/schemas/GroupID" }
        groupName: { $ref: "#/components/schemas/GroupName" }
        UserList: { $ref: "#/components/schemas/UsersList" }

    Photo: 
      description: Represent a photo
      type: object
      properties:
        photoUrl:
          type: string
          format: uri
          example: "https://example.com/photo.jpg"

    Message:
      description: Represent a message 
      type: string
      example: "Hi!"
      pattern: '^.*?$'
      minLength: 1
      maxLength: 500

    MessageID:
      description: A message's id
      type: integer
      example: 123456

    Comment:
      description: Represent a comment
      type: string
      example: "😀👍🌟"
      pattern: '^[\u1F300-\u1F6FF\u1F900-\u1F9FF\u2600-\u26FF\u2700-\u27BF\uFE0F]+$'
      minLength: 1
      maxLength: 10

    CommentID:
      description: A comment's id
      type: integer
      example: 123456

#    Messages:
#      type: object
#      properties:
#        conversationId: { $ref: "#/components/schemas/ConversationID" }
#        sender: { $ref: "#/components/schemas/User" }
#        content : { $ref: "#/components/schemas/Message" }
#        timestamp: {$ref: "#/components/schemas/Timestamp" }    

    Conversation:
      description: Represent a conversation, includes foto, messages and comments
      type: object
      properties:
        photo: { $ref: "#/components/schemas/Photo" }        
        conversationId: { $ref: "#/components/schemas/ConversationID" }
        sender: { $ref: "#/components/schemas/User" }
        content : { $ref: "#/components/schemas/Message" }
        comment: {$ref: "#/components/schemas/Comment"}
        timestamp: {$ref: "#/components/schemas/Timestamp" }        

    ConversationID:
      description: A conversation's id 
      type: integer
      example: 123456

    listOfConversations:
      description: Represent a list of conversations
      type: array
      minItems: 0
      maxItems: 64
      items: { $ref: "#/components/schemas/Conversation" }

    Timestamp:
      title: Timestamp
      description: The timestamp of the message.
      type: string
      format: date-time
      pattern: "^[0-9]{4}-[0-9]{2}-[0-9]{2}T[0-9]{2}:[0-9]{2}:[0-9]{2}$"
      example: "2024-12-31T23:59:59"
      minLength: 19
      maxLength: 19

    StatusInfo:
      title: Status Info
      type: object
      properties:
        status:
          type: string
          description: "The status level"
          enum:
            - info
            - warning
            - critical
            - ""
        title:
          type: string
          description: "The title of the status information"
        message:
          type: string
          description: "A descriptive message about the status"
        url:
          type: string
          description: "A URL associated with the status information"
      example:
        status: "warning"
        title: "High CPU Usage"
        message: "The CPU usage has exceeded 90% for the last 5 minutes."
        url: "https://example.com/status/high-cpu-usage"
    

  responses:
    Unauthorized: #401
      description: The access token is missing or it's expired

    BadRequest: #400
      description: The request was not compliant with the documentation (eg. missing fields, etc)
    
    Forbidden: #403
      description: The user is not allowed to perform the action.

    InternalServerError: #500
      description: The server encountered an internal error. Further info in server logs

  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer